defaultNamespace: limitless-staging

helm:
  chart: ""
  releaseName: limitless-staging
  valuesFiles:
    - values.staging.yaml

targetCustomizations:
  - name: local
    clusterSelector:
      matchLabels:
        management.cattle.io/cluster-display-name: local
