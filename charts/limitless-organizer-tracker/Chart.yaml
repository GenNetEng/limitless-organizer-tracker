apiVersion: v2
name: limitless-organizer-tracker
description: Limitless TCG organizer application tracker and analytics dashboard
type: application
version: 0.1.0
appVersion: "0.3.0"

dependencies:
  - name: redis
    version: "~20.x"
    repository: https://charts.bitnami.com/bitnami
    condition: redis.enabled
